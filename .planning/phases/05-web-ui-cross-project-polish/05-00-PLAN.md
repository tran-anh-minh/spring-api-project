---
phase: 05-web-ui-cross-project-polish
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_web.py
  - tests/test_daemon.py
  - tests/test_cross.py
  - tests/test_export.py
  - tests/test_cli_phase5.py
  - tests/test_server_phase5.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - LEARN-13
  - CROSS-01
  - CROSS-02
  - EXPORT-01
  - EXPORT-03
  - MCP-06
  - CLI-05

must_haves:
  truths:
    - "Every Phase 5 requirement has a failing test stub before implementation begins"
    - "Running pytest on the new test files produces xfail results, not errors"
  artifacts:
    - path: "tests/test_web.py"
      provides: "Test stubs for UI-01 through UI-06"
      contains: "test_web_app_serves_index"
    - path: "tests/test_daemon.py"
      provides: "Test stubs for LEARN-13 scheduler"
      contains: "test_scheduler_starts"
    - path: "tests/test_cross.py"
      provides: "Test stubs for CROSS-01, CROSS-02"
      contains: "test_open_cross_store"
    - path: "tests/test_export.py"
      provides: "Test stubs for EXPORT-03 formatters"
      contains: "test_markdown_exporter"
    - path: "tests/test_cli_phase5.py"
      provides: "Test stubs for CLI-05 and EXPORT-01"
      contains: "test_status_command"
    - path: "tests/test_server_phase5.py"
      provides: "Test stubs for MCP-06 tools"
      contains: "test_lint_tool"
  key_links: []
---

<objective>
Create Wave 0 test stubs for all Phase 5 requirements so every subsequent plan has a pre-existing failing test to flip green.

Purpose: Nyquist compliance — every requirement has an automated test before implementation begins.
Output: 6 test files with xfail stubs covering all Phase 5 verification map entries from 05-VALIDATION.md.
</objective>

<execution_context>
@D:/Working/Project/db-wiki/.claude/get-shit-done/workflows/execute-plan.md
@D:/Working/Project/db-wiki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-ui-cross-project-polish/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for web UI and daemon</name>
  <files>tests/test_web.py, tests/test_daemon.py</files>
  <read_first>
    - tests/conftest.py
    - .planning/phases/05-web-ui-cross-project-polish/05-VALIDATION.md
    - tests/test_server.py (for existing test patterns)
  </read_first>
  <action>
Create tests/test_web.py with pytest.mark.xfail stubs:
- test_web_app_serves_index: import create_web_app from db_wiki.web.app, create starlette TestClient, GET "/" returns 200 with HTML containing "DB Wiki" (UI-01)
- test_graph_api_initial_load: GET /api/graph returns 200 JSON with "nodes" and "edges" keys (UI-02)
- test_graph_api_expand_node: GET /api/graph?node=1&depth=1 returns 200 JSON with nodes array (UI-03). Assert depth param is validated <= 5 (T-05-01)
- test_wiki_api: GET /api/wiki?entity_id=1 returns 200 JSON with "name", "type", "l1_content" keys (UI-04)
- test_node_confidence_field: GET /api/graph returns nodes with "opacity" field present (UI-05)
- test_gap_node_flag: GET /api/graph returns gap nodes with "borderDashes" field (UI-06)
- test_search_api: GET /api/search?q=orders returns 200 JSON with "results" array (UI-03)
- test_dashboard_api: GET /api/dashboard returns 200 JSON with "coverage_pct", "gap_count", "conflict_count" keys (EXPORT-01)

Create tests/test_daemon.py with pytest.mark.xfail stubs:
- test_scheduler_starts: import DaemonScheduler from db_wiki.daemon.scheduler, call start(), assert _thread is alive (LEARN-13)
- test_scheduler_stops: start then stop scheduler, assert _stop_event is set (LEARN-13)
- test_scheduler_uses_instance_scheduler: assert DaemonScheduler uses schedule.Scheduler() not global schedule module (LEARN-13)
- test_scheduler_adaptive_frequency: verify scheduler adjusts intervals based on gap count (D-05)

All stubs use @pytest.mark.xfail(reason="Phase 5 Wave 0 stub — not yet implemented", strict=True).
  </action>
  <verify>
    <automated>uv run pytest tests/test_web.py tests/test_daemon.py --co -q 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_web.py contains "def test_web_app_serves_index"
    - tests/test_web.py contains "def test_graph_api_initial_load"
    - tests/test_web.py contains "def test_graph_api_expand_node"
    - tests/test_web.py contains "def test_wiki_api"
    - tests/test_web.py contains "def test_node_confidence_field"
    - tests/test_web.py contains "def test_gap_node_flag"
    - tests/test_daemon.py contains "def test_scheduler_starts"
    - tests/test_daemon.py contains "def test_scheduler_stops"
    - All stubs decorated with @pytest.mark.xfail
  </acceptance_criteria>
  <done>12 xfail test stubs exist across test_web.py and test_daemon.py, all collected by pytest without import errors</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for cross-project, export, CLI, and MCP</name>
  <files>tests/test_cross.py, tests/test_export.py, tests/test_cli_phase5.py, tests/test_server_phase5.py</files>
  <read_first>
    - tests/conftest.py
    - .planning/phases/05-web-ui-cross-project-polish/05-VALIDATION.md
    - tests/test_cli.py (for CLI test patterns)
    - tests/test_server_phase4.py (for MCP tool test patterns)
  </read_first>
  <action>
Create tests/test_cross.py with pytest.mark.xfail stubs:
- test_open_cross_store: import open_cross_store from db_wiki.cross.store, call it with tmp_path, assert returns sqlite3.Connection (CROSS-01)
- test_cross_schema_tables: open cross store, verify cross_patterns table exists (CROSS-01)
- test_cross_penalty: import get_cross_patterns from db_wiki.cross.reader, verify returned patterns have adjusted confidence < original (CROSS-02)
- test_push_patterns: import push_patterns_to_cross from db_wiki.cross.export, verify patterns are inserted into cross.db (CROSS-01, D-07)

Create tests/test_export.py with pytest.mark.xfail stubs:
- test_markdown_exporter: import MarkdownExporter from db_wiki.export.markdown, call export(), assert output contains "# " heading (EXPORT-03)
- test_mermaid_exporter: import MermaidExporter from db_wiki.export.mermaid, call export(), assert output contains "erDiagram" (EXPORT-03)
- test_json_schema_exporter: import JsonSchemaExporter from db_wiki.export.json_schema, call export(), assert output is valid JSON with "tables" key (EXPORT-03)
- test_ddl_annotated_exporter: import AnnotatedDDLExporter from db_wiki.export.ddl_annotated, call export(), assert output contains "CREATE TABLE" and "-- " comments (EXPORT-03)

Create tests/test_cli_phase5.py with pytest.mark.xfail stubs:
- test_status_command: invoke CLI "status" command via CliRunner, assert output contains "Coverage" (EXPORT-01, CLI-05)
- test_serve_command_exists: verify "serve" command registered in Typer app (CLI-05)
- test_export_command: invoke CLI "export" command, assert it runs without error (CLI-05)

Create tests/test_server_phase5.py with pytest.mark.xfail stubs:
- test_lint_tool: call lint MCP tool, assert returns string (MCP-06)
- test_history_tool: call history MCP tool, assert returns string (MCP-06)
- test_export_tool: call export MCP tool, assert returns string with export path (MCP-06)
- test_loop_tool: call loop MCP tool, assert returns learning loop summary (MCP-06)

All stubs use @pytest.mark.xfail(reason="Phase 5 Wave 0 stub — not yet implemented", strict=True).
  </action>
  <verify>
    <automated>uv run pytest tests/test_cross.py tests/test_export.py tests/test_cli_phase5.py tests/test_server_phase5.py --co -q 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cross.py contains "def test_open_cross_store"
    - tests/test_cross.py contains "def test_cross_penalty"
    - tests/test_export.py contains "def test_markdown_exporter"
    - tests/test_export.py contains "def test_mermaid_exporter"
    - tests/test_export.py contains "def test_json_schema_exporter"
    - tests/test_export.py contains "def test_ddl_annotated_exporter"
    - tests/test_cli_phase5.py contains "def test_status_command"
    - tests/test_server_phase5.py contains "def test_lint_tool"
    - All stubs decorated with @pytest.mark.xfail
  </acceptance_criteria>
  <done>15 xfail test stubs exist across 4 test files, all collected by pytest without import errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Wave 0 creates test files only — no production code, no trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | Test stubs only — no security-relevant code in this plan |
</threat_model>

<verification>
All 6 test files are collected by pytest without import errors. All stubs are marked xfail.
</verification>

<success_criteria>
- 27 xfail test stubs across 6 files covering every Phase 5 verification map entry
- `uv run pytest tests/test_web.py tests/test_daemon.py tests/test_cross.py tests/test_export.py tests/test_cli_phase5.py tests/test_server_phase5.py --co -q` lists all test names
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-ui-cross-project-polish/05-00-SUMMARY.md`
</output>
